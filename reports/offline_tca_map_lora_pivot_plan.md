# Offline TCA-Map / LoRA Pivot Plan

This report-only plan is used after the checkpoint/task provenance resolver blocks learned-policy LIBERO rollout scaling for the current base SmolVLA checkpoint.

Purpose:

- keep the paper path moving without overclaiming weak learned-policy rollout diagnostics,
- consolidate the real-LIBERO offline proxy evidence already produced for ActionMap, TCA-Map, Distributional TCA-Select, and required LoRA tracks,
- identify remaining gaps before any paper-grade claim or larger experiment.

Expected decision:

- if provenance blocks current-checkpoint learned-policy rollouts and offline head/LoRA reports are present, select an offline evidence table and gap report,
- keep learned-policy rollout scaling blocked until either a LIBERO-action-aligned checkpoint source is resolved or a bounded training/adaptation path is validated,
- keep standard success, benchmark success, SOTA, and paper-grade claims blocked.

Command:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\123_plan_offline_tca_map_lora_pivot.ps1
```
