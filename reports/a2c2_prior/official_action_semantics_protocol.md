# Frozen A2C2 official-action-semantics protocol

The historical strict-raw-bound result remains preserved. For this one
authorized correction, raw `[-1,1]` exceedance is diagnostic; validity follows
the released evaluator and native controller path. Raw actions must be finite
7D values, no wrapper clip may be added, every action must be accepted, and
native arm, gripper, actuator, torque, and simulator state must remain valid.

Before outcomes, Prior-specific instability is frozen as a `>=0.05` increase
in maximum exceedance plus either a `>=0.02` raw-element exceedance-fraction
increase or `>=0.10` native arm-clip-step increase, reproduced on both
development identities. Any raw magnitude above `2.0` is invalid rather than
arbitrarily accepted.

The smoke uses task/reset `(2,11)` and `(6,11)`, matched delayed Base/Prior,
and exactly 80 steps per trace. The runner neither checks nor persists success,
done, or reward. Only `CORRECTED_A2C2_OFFICIAL_SEMANTICS_SMOKE_PASS` opens the
unchanged tasks `0,4,8`, resets `5..9`, 45-row scientific panel.
