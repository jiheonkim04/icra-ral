# Autonomous Until Paper Reviewer Log

## 2026-07-12 Startup

Reviewer B treats the following as closed unless materially redefined:

- ECHO-style candidate selection/ranking with same-state candidate headroom;
- PhaseBarrier-style phase-conditioned action projection;
- CensorCredit-style temporal trust heads and hold blending;
- ISAC-style paired intervention/correction chunk fine-tuning without real intervention data.

The reviewer will not reject a new method solely because it shares a broad family with recent work. A literature-only kill requires near-exact overlap, equivalence to a trivial baseline, or essential unavailable resources.

## 2026-07-12 Candidate Attack

Reviewer B attacked all three epoch-1 candidates.

- `DICD-VLA`: high DEFLECT/TIC-VLA overlap risk but not killed before proposal because the proposed mechanism is explicit delay-indexed chunk distillation, not flow-likelihood stale/fresh preference tuning or slow/fast VLA architecture.
- `SCOP-VLA`: not selected due high overlap with LIBERO-Occ and memory-augmented VLA work.
- `CLC-VLA`: not selected due high overlap with CAG/LIBERO-CF and CAST plus label-validity risk.
