# HASTE-VLA Stage 0A Result

Decision: `HASTE_STAGE_0A_IMPLEMENTATION_FAILURE`.

The frozen runner wrote Linux PID `295` and then exited `1` before manifest
persistence. Manifest hashing received NumPy normalization arrays that the
canonical JSON helper could not serialize.

No partial, manifest, feature cache, adapter checkpoint, auxiliary-head
checkpoint, model inference, simulator row, or confirmatory record exists.
Persisted row count is `0`; therefore duplicate, missing, extra, and key-set
acceptance are inapplicable, and no row is accepted as evidence.

This is a pre-manifest implementation failure, not a scientific result or
scientific kill. Stage 0B, resume, rerun, repair, and HASTE rescue are not
allowed under the frozen protocol.
