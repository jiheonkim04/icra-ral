# Epoch 8 Terminal Handoff

State: `LOCAL_PROGRAM_BLOCKED_WITH_AUDITABLE_EXHAUSTION`

Branch: `codex/epoch8-language-grounding-convergence`

Campaign source checkpoint: `4422a95be5412bdb774287fce9f42004a8631f39`

Epoch 8 scientific checkpoint: `a1d0d7f262038d43cbedf7640373c411b8f2e383`

The exact checkpoint containing this handoff is recorded by the subsequent
checkpoint commit and remote branch head. No paper is authorized because
Epoch 8 has zero official closed-loop Ours episodes and no benchmark result
across several competent policies.

## What finished

- Rebuilt and corrected the inherited route ledger: 109 audited entries,
  exact closure scopes, evidence classes, role-specific lower-bound counts,
  and raw Candidate A/R hash checks.
- Refreshed the 2025--2026 language-grounding literature and official-artifact
  boundary.
- Froze disjoint language splits and audited 30 semantic-selectivity pairs.
- Implemented and validly falsified PCAT with real CUDA training.
- Rotated through altered-dynamics feedback feasibility, two-shard
  actual-arrival resources, and active hidden-mass grounding.
- Preserved every invalid attempt and applied only two semantically null
  repairs: explicit WSL lock propagation for the resource smoke, and identical
  probe-prefix enforcement for the probe-belief gate.
- Regenerated the ledger with every new outcome and claim limit.

## Active and finished jobs

There are no active experiment workers. Every Epoch 8 job is complete or
resource-terminated, and every accepted result is atomically persisted. The
two concurrency scientific panels remain unobserved at 0/40 each.

## Critical results

- PCAT: `reports/epoch8_pcat_stage0/result.json`
- PCAT adjudication: `reports/epoch8_pcat_stage0_adjudication.json`
- Feedback expert: `reports/epoch8_latent_dynamics_feedback_development.json`
- Two-shard resource report:
  `reports/epoch8_two_shard_actual_arrival/run_20260720_2039_kst_resource_repair1/two_shard_resource_smoke_host.json`
- Hidden-mass Base screen:
  `reports/epoch8_active_latent_property/stage_minus1_result.json`
- Probe-belief valid result:
  `reports/epoch8_active_property_probe_belief_stage0_repair1.json`
- Probe-return result:
  `reports/epoch8_active_property_probe_return_result.json`
- Full terminal reasoning: `reports/epoch8_terminal_audit.md`
- Machine state: `reports/epoch8_campaign_state.json`

## Safe resume

On a clean 48 GB-or-larger equivalent host, use a fresh two-shard run ID,
execute the preflight command in the campaign state, then its host-monitored
resource smoke. Only a valid pass authorizes `--mode run-all`. This is a new
two-shard study and must never be described as the archived four-shard result.

An active-property restart is a new method cycle, not a repair: freeze a legal
visual-feedback probe-return-select controller and a causally distinct temporal
response representation on new development identities, then keep a fresh
confirmation band sealed. Do not convert the visible 6/6 paired score ordering
into an unregistered ranking rescue.

The protected rollout directories remain unchanged and untracked.
