# KITE-VLA Researcher A Rebuttal

Date: 2026-07-15 KST

Frozen proposal SHA-256:
`FA00DE56D14E4C69388BE1642F7D52153841D58E77FD5A3F5C68B6C624A152B8`.

Decision: `KITE_REBUTTAL_PASS_TO_MATHEMATICAL_AUDIT`.

Researcher A accepts every essential Reviewer B condition without changing the
central method.

1. The key ablation is now `cumulative_action_target`, not endpoint-only KITE.
   It matches cumulative demonstration commands at both horizons without
   `ee_states` or `F_H`.
2. `F_5` and `F_20` are global task-agnostic discovery-only ridge affine
   operators. No task identity or learned policy representation enters them.
3. Reports use `empirical action-to-state realization operator`, not forward
   kinematics, dynamics model, or SE(3) physics.
4. Future states are training-only aligned labels, boundary masked, and absent
   from validation selection inputs other than preregistered metrics.
5. The full manifest payload must JSON round-trip and reproduce its hash before
   a detached worker may launch.
6. Stage 0A records objective scales and verifies that `L_kite` has a finite
   nonzero gradient path through generated clean actions into expected LoRA
   targets.
7. The first serious comparison has exactly Base, transparent GeoPredict
   proxy, KITE, cumulative-action-target ablation, and standard LoRA.
8. KITE uses no event labels, hazards, HASTE code, action smoothing, memory, or
   inference correction.

The rank-8 path is not a search configuration. It is allowed once only if a
valid fixed-rank micro-fit proves
`LOW_COMPUTE_PARAMETERIZATION_INSUFFICIENT`; it may not follow a data,
headroom, design, implementation, or confirmatory result.
