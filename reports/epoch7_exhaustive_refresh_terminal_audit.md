# Epoch 7 Exhaustive Refresh Terminal Audit

Adjudicated: 2026-07-20 (Asia/Seoul)

Terminal decision: `HARD_EXTERNAL_BLOCKER_REQUIRES_USER`

## Outcome

Epoch 7 does not have a reviewer-defensible RA-L paper candidate. No Ours has
positive official closed-loop evidence against Base, Prior, ablation, and
control under a paperability contract, so paper generation is not authorized.
The campaign is not converted into a negative-results paper.

Four evidence-driven portfolio cycles were completed after the inherited
95-route history audit:

1. Language equivalence/selectivity verified a real X-VLA problem (`30/30`
   canonical versus `19/30` paraphrase), but the frozen real-action mechanism
   failed (`2/30` ranking violations versus `6/30` required).
2. Latent object/environment dynamics passed simulator feasibility but failed
   legal three-family expert headroom. Policy-RNG reliability was rejected as
   a one-change variant of the unresolved schedule route and as overlapping
   SDN.
3. Stability-qualified completion found three suffix-recoverable transient
   successes, all explained by one `On` predicate family. Typed non-gripper
   contact transitions then passed label validity but failed both visual
   predictability and oracle action-headroom gates.
4. Consequence adaptation, demonstration temporal integrity, executable-policy
   monitoring, and action-chunk continuity were refreshed against current
   primary work and local controls. They are duplicated, locally answered, or
   lack a genuine local problem instance.

Method, benchmark/evaluation, data, and systems archetypes have therefore been
revisited. The conclusion is bounded to the retained assets and the audited
literature through 2026-07-20; it is not a universal statement that VLA
research has no open problems.

## Only serious unresolved path

The exact archived schedule-invariant stochastic VLA evaluation remains
scientifically unresolved and competed normally only after the fresh audit.
Its frozen Stage 0 established action-level dependence, but the official
closed-loop panel has `0/40` episodes executed. This is zero observations, not
0% success. Ours was never authorized or evaluated.

The claim-defining intervention requires four simultaneously live official
LIBERO environments feeding one shared X-VLA model. On the current 24.87 GB
host, the environments alone reached 85.16% used RAM before model load and
crossed the frozen 82% ceiling. The conservative full-path projection is
28.66 GB used; remaining below 82% requires 34.96 GB total physical memory.
A 32 GiB machine is not certified by that projection. A clean 48 GB-or-larger
host is the smallest standard tier with defensible headroom.

The frozen conditions remain:

- four simultaneous environment processes;
- one shared model and one model forward per batch;
- unchanged tasks, identities, schedules, seeds, and 82% host ceiling;
- zero WSL swap and no CPU/disk model offload;
- outcome-free resource smoke before any scientific episode.

Serial execution, two-shard execution, state multiplexing, or a changed
schedule is not a resource repair. Any such change is a new scientific
protocol and cannot inherit the archived preregistration.

## Exhaustion of safe local alternatives

| Route | Why no next local experiment is authorized |
| --- | --- |
| Consequence-conditioned adaptation | Reflective VLA directly occupies the mechanism; local RAC Stage B returned full/Base/proxy `1/40` and ablation/inverse `2/40`. |
| Temporal-integrity correction | Retained LIBERO data are index-aligned simulator trajectories without independent timestamps; a synthetic shift does not establish a real local premise. Green-VLA and DEFLECT already occupy alignment/stale-state mechanisms. |
| Executable-policy integrity/monitoring | Same Weights, Different Robot and SafeContract are direct current Priors; local ExecSpec repair tied diagonal affine calibration at `17/19`. |
| Chunk continuity/speed/commitment | SEAM, TempoVLA, AAC, and DEFLECT occupy the mechanisms; local DICD scored `1/10` versus direct chunk index `2/10`, and EAC scored `29/40` versus Base/AAC/ablation `30/40`. |
| Quantized/deployment VLA | Omega-QVLA directly supplies W4A4 backbone/action-head quantization and needs a 40 GB A100 to build; its published evaluation is multi-GPU. |
| Language/grounding | A real Base gap exists, but the selected mechanism failed and augmentation, canonicalization, CAG-style guidance, and renamed energy ranking are prohibited/directly occupied rescues. |
| Latent dynamics benchmark | Frozen legal expert replay established altered-condition headroom for only two collapsed families, below the prespecified three-family requirement. |
| Persistent completion | All three disagreements were one `On` predicate family, below the frozen cross-mechanism gate. |
| Typed contact topology | Valid Stage 0B returned `STAGE0_TRIVIAL_EQUIVALENCE`; visual probes lost to nonvisual controls and typed labels did not improve oracle arm prediction. |
| Serial evaluation runtime | Useful infrastructure but no independent robotics contribution beyond vla-eval and existing local implementation. |

No safe download, local retraining, serial rollout, additional reset scan, or
post-hoc threshold/task change resolves these scientific vetoes. The only
remaining serious empirical question needs external host memory.

## Internal review

### Executor

- Actual Epoch 7 execution included X-VLA inference and official closed-loop
  problem verification, expert action replay, simulator-only preflights, and
  real Stage 0 diagnostics. All stage results are preserved atomically.
- Candidate K's final execution used 7,888 aligned rows and three fixed seeds;
  zero policy rollouts or forbidden outcome accesses occurred.
- No paper source/PDF was created because the paper-readiness gate never
  passed.

### Skeptical reviewer

- The literature audit is necessarily bounded and fast-moving; a newly
  released artifact could change comparator feasibility.
- Some early historical routes were underpowered, but later richer methods and
  controls addressed the same families. Reopening them solely because no paper
  emerged would be outcome-driven.
- The schedule route may ultimately show action differences with no task-level
  effect, so additional hardware enables an experiment, not a paper promise.
- All evidence is simulation-only.

### Adjudicator

- No locally obtainable single missing evidence item can promote a candidate.
- Weak or duplicated candidates are not promoted to avoid the blocker.
- The schedule route is not scientifically closed; its exact next gate is
  externally resource-blocked.
- Final classification: `HARD_EXTERNAL_BLOCKER_REQUIRES_USER`.

## Smallest user-controlled continuation

Provide or authorize a clean scientifically equivalent host with at least
48 GB physical RAM, zero WSL swap, no CPU/disk model offload, and the unchanged
82% ceiling. Then run a fresh immutable four-environment resource smoke before
any of the 40 official episodes. The canonical archived command is:

```powershell
git switch codex/epoch6-ral-submission-convergence-v2
git pull --ff-only
wsl --shutdown
powershell -ExecutionPolicy Bypass -File scripts/monitor_epoch6_schedule_closed_loop_smoke.ps1 -RunId <fresh_immutable_run_id>
```

Alternatively, the user may explicitly authorize a new independent protocol
with different concurrency, but it must be treated as a new study and cannot
reuse the frozen four-shard evidence as if unchanged.

## Preservation

The user-owned untracked directories `rollouts/2026_07_17/` and
`rollouts/2026_07_18/` remain untouched. No archived Epoch 5/6 evidence,
checkpoint, dataset, cache, or protected rollout was deleted or rewritten.
